{
  "packages": ["packages/*"],
  "version": "independent",
  "command": {
    "publish": {
      "npmClient": "npm"
    },
    "version": {
      "allowBranch": ["master", "next", "feat/*", "bug/*"],
      "message": "chore(release): publish",
      "conventionalCommits": true,
      "createRelease": "github"
    }
  },
  "ignoreChanges": ["**/*.md", ".github/**", "**/tests/**", "automation/**"]
}
