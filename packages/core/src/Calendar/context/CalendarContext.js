import React from "react";

export const HvCalendarContext = React.createContext({});

export const HvCalendarContextProvider = HvCalendarContext.Provider;
export const HvCalendarContextConsumer = HvCalendarContext.Consumer;
export default HvCalendarContext;
