const styles = (theme) => ({
  root: {
    padding: theme.hvSpacing(0, "sm", "sm", "sm"),
    flex: "none",
  },
  textContent: {
    marginLeft: "42px",
    marginRight: "42px",
  },
});

export default styles;
