const styles = (theme) => ({
  root: {
    padding: theme.hvSpacing(0, "sm", "sm", "sm"),
    flex: 1,
  },
  textContent: {
    margin: "0 42px",
    flex: 1,
    overflowY: "auto",
  },
});

export default styles;
