const styles = theme => ({
  root: {
    display: "inline-block"
  },
  infoDisabled: {
    color: theme.hv.palette.atmosphere.atmo5
  },
  gutter: {
    marginBottom: 8
  }
});

export default styles;
