const styles = theme => ({
  root: {
    display: "inline-block"
  },
  labelDisabled: {
    color: theme.hv.palette.atmosphere.atmo5
  },
  childGutter: {
    marginBottom: 6
  }
});

export default styles;
