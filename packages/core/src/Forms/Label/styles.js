const styles = (theme) => ({
  root: {
    display: "inline-block",
  },
  labelDisabled: {
    color: theme.hv.palette.atmosphere.atmo7,
  },
  childGutter: {
    marginBottom: 8,
  },
});

export default styles;
