import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { ColumnResizeStory } from "./TableSamples.stories";

<Meta title="Visualizations/Table/Table Samples/Column Resize" />

# Column Resize

Columns can be resized using the `useHvResizeColumn` hook, which leverages the react-table capabilities. Resize, although not an accessible functionality,
it allows columns to be resizable via dragging column right border.

<Canvas>
  <Story name="Column Resize" story={ColumnResizeStory} />
</Canvas>
