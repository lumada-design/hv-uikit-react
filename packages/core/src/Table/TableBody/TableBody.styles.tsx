import { createClasses } from "@core/utils/classes";

export const { staticClasses, useClasses } = createClasses("HvTableBody", {
  root: { backgroundColor: "inherit", position: "relative", zIndex: 0 },
});
