import styled from "@emotion/styled";
import { HvLoading } from "@hitachivantara/uikit-react-core";

export const StyledLoading = styled(HvLoading)({
  display: "flex",
  alignItems: "center",
  width: "100%",
  height: "100%",
});

export const StyledMain = styled("main")({
  flex: 1,
});
