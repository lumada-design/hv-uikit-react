import { HvBox } from "@hitachivantara/uikit-react-core";
/**
 * A someComponent is a design element that expands in place to expose hidden information.
 */
export const HvSomeComponent = () => {
  return <HvBox>SomeComponent</HvBox>;
};
