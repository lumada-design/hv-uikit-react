import { Meta, Story, Preview } from "@storybook/addon-docs";
import IconLibrary from "./Library";
import { ActualSize, FemaleRunning } from "@hitachivantara/uikit-react-icons";
import * as iconComponentList from "@hitachivantara/uikit-react-icons/dist/icons";
import * as iconPictogramComponentList from "@hitachivantara/uikit-react-icons/dist/pictograms";
import { MaturityStatus } from "../../../.storybook/blocks/MaturityStatus";
import componentDefinitions from "../../versions";

<Meta
  title="Foundation/Icons"
  parameters={{
    dsVersion: componentDefinitions.dsVersion.v3,
  }}
/>

# Icon library

<MaturityStatus />

You can import our React icons from the icon library as follows (example for the `ActualSize` icon):

```js
import { ActualSize } from "@hitachivantara/uikit-react-icons";
```

And to use them:

```jsx
<ActualSize />
```

This will be the result:

<Preview>
  <ActualSize />
</Preview>

<Preview>
  <Story name="Main">
    <IconLibrary icons={iconComponentList} />
  </Story>
</Preview>

# Pictograms library

You can import our React pictograms from the icon library as follows (example for the `FemaleRunning` icon):

```js
import { FemaleRunning } from "@hitachivantara/uikit-react-icons";
```

And to use them:

```jsx
<FemaleRunning />
```

This will be the result:

<Preview>
  <FemaleRunning />
</Preview>

<Preview>
  <Story name="Pictograms">
    <IconLibrary icons={iconPictogramComponentList} widerSpacing />
  </Story>
</Preview>
