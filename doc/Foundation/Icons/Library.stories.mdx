import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import IconLibrary from "./Library";
import { ActualSize } from "@hitachivantara/uikit-react-icons";

<Meta title="Foundation/Icons/Library" />

# Icon library

You can import our React icons from the icon library as follows (example for the `ActualSize` icon):

```js
import { ActualSize } from "@hitachivantara/uikit-react-icons";
```

And to use them:

```jsx
<ActualSize />
```

This will be the result:

<Preview>
  <ActualSize />
</Preview>

<Preview>
  <Story name="Icons">
    <IconLibrary />
  </Story>
</Preview>
