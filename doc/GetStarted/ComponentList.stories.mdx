import { Meta } from "@storybook/addon-docs";

import { HvTypography } from "@hv/uikit-react-core";

import ComponentsTable from "./ComponentVersioningTable/ComponentsTable";
import coreTableData from "./ComponentVersioningTable/CoreComponentData";
import coreComponentTableConfig from "./ComponentVersioningTable/coreComponentTableConfig";

import labComponentData from "./ComponentVersioningTable/LabComponentData";
import labComponentTableConfig from "./ComponentVersioningTable/labComponentTableConfig";

<Meta title="Get Started/Component List" />

# Component List

<p>
  DS 3.x is implemented by UI Kit v3, while DS 1.x is implemented by&nbsp;
  <a href="https://lumada-design.github.io/uikit/v2.x/?path=/docs/*">UI Kit v2</a>. The use of UI Kit
  v1 is strongly discouraged and is only supported on products that already used it before the release
  of UI Kit v2. You can find its documentation <a href="https://lumada-design.github.io/uikit/v1.x/">
    here
  </a>.
</p>

<br />

<ComponentsTable tableData={coreTableData} tableColumnsConfig={coreComponentTableConfig} />

<br />
<br />

## Lab

<p>
  Lab package contains the React components <b>contributed by external teams</b> for specific use
  cases and <b>not fully compliant</b> with the Design System guidelines. For this reason we do not
  specify a DS version which they might be partially compliant.
</p>

<br />

<ComponentsTable tableData={labComponentData} tableColumnsConfig={labComponentTableConfig} />
