{
  "name": "hv-ui",
  "version": "0.0.0",
  "description": "A collection of Themes and UI Components for the Hitachi Vantara Design System.",
  "private": true,
  "author": {
    "name": "Hitachi Vantara IOTA Team"
  },
  "repository": {
    "type": "git",
    "url": "https://10.76.48.133/hv-design-system/hv-ui.git"
  },
  "devDependencies": {
    "lerna": "^3.5.1",
    "sleep-ms": "^2.0.1"
  },
  "scripts": {
    "clean": "lerna clean --yes",
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --parallel",
    "dev": "lerna run dev --stream",
    "lint": "lerna run lint --stream",
    "lint:jenkins": "lerna run lint:jenkins --stream",
    "test": "lerna run --ignore with-cra test --stream",
    "link": "lerna run link --parallel",
    "publish:prerelease": "lerna publish prerelease --yes",
    "publish:minor": "lerna publish minor --yes",
    "publish:major": "lerna publish major --yes"
  },
  "dependencies": {
    "npx": "^10.2.0"
  }
}
