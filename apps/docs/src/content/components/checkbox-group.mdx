import {
  checkBoxGroupClasses as classes,
  HvCheckBox,
  HvCheckBoxGroup,
} from "@hitachivantara/uikit-react-core";

import { Header } from "../../components/Header";

<Header
  params={{ name: "CheckBoxGroup", classes }}
  Component={HvCheckBoxGroup}
  componentName="HvCheckBoxGroup"
  controls={{
    label: { defaultValue: "Type" },
    description: { defaultValue: "Visualization type" },
    orientation: { defaultValue: "vertical" },
    required: { defaultValue: true },
    disabled: { defaultValue: false },
    readOnly: { defaultValue: false },
  }}
  componentProps={{
    defaultChecked: false,
  }}
>
  <HvCheckBox label="Bar Chart" value="1" />
  <HvCheckBox label="Line Chart" value="2" />
  <HvCheckBox label="Pie Chart" value="3" />
</Header>

### Controlled

Controlled radio button group. Choosing the first option will result in an invalid state of the Radio group.

```tsx live
import { useState } from "react";

export default function Demo() {
  const [value, setValue] = useState("2");
  const [status, setStatus] = useState<HvFormStatus>("standBy");

  return (
    <HvCheckBoxGroup
      label="Choose the best radio button"
      value={value}
      status={status}
      statusMessage="Don\'t select 'None'!"
      onChange={(evt, newValue) => {
        setValue(newValue);
        setStatus(newValue.includes("none") ? "invalid" : "valid");
      }}
    >
      <HvCheckBox label="None" value="none" />
      <HvCheckBox label="Radio 1" value="1" />
      <HvCheckBox label="Radio 2" value="2" />
    </HvCheckBoxGroup>
  );
}
```

### Related components

- [`HvFormElement`](/components/form-element)
- [`HvSelectionList`](/components/selection-list)
- [`HvCheckbox`](/components/radio)
