import {
  simpleGridClasses as classes,
  HvPanel,
  HvSimpleGrid,
} from "@hitachivantara/uikit-react-core";

import { Header } from "../../components/Header";

> [!NOTE]
>
> Please check the [`HvGrid`](/components/grid) component documentation for more
> advanced grid configurations.

<Header
  params={{ name: "SimpleGrid", classes }}
  Component={HvSimpleGrid}
  componentName="HvSimpleGrid"
  controls={{
    cols: {
      defaultValue: 2,
    },
    spacing: {
      defaultValue: "sm",
    },
  }}
>
  {[...Array(6).keys()].map((i) => (
    <HvPanel>Item {i + 1}</HvPanel>
  ))}
</Header>

### Breakpoints

Use the `breakpoints` prop to define the number of columns and spacing for different widths.

```tsx live
<div className="w-full">
  <HvSimpleGrid
    cols={2}
    breakpoints={[
      { minWidth: 1600, cols: 4, spacing: "md" },
      { minWidth: 1300, cols: 3, spacing: "md" },
      { minWidth: 1100, cols: 2, spacing: "sm" },
      { minWidth: 800, cols: 1, spacing: "sm" },
    ]}
  >
    {[...Array(6).keys()].map((i) => (
      <HvPanel>Item {i + 1}</HvPanel>
    ))}
  </HvSimpleGrid>
</div>
```

### Related components

- [`HvGrid`](/components/grid)
