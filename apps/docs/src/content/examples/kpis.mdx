import { CodeBlock } from "../../components/code/CodeBlock";

import dataLoading from "../../examples/kpis/DataLoading?raw";
import selectable from "../../examples/kpis/Selectable?raw";
import semantic from "../../examples/kpis/Semantic?raw";

<div className="mt-20">
  <CodeBlock layout="expandable" code={semantic} />
  <CodeBlock layout="expandable" code={selectable} />
  <CodeBlock layout="expandable" code={dataLoading} />
</div>
