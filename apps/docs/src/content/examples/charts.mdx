import { CodeBlock } from "../../components/code/CodeBlock";
import { PopupLayoutPreview } from "../../components/code/PopupLayoutPreview";

import donutChartTotal from "../../examples/charts/DonutChartTotal?raw";
import gaugeChart from "../../examples/charts/GaugeChart?raw";
import lineChartStreamingCode from "../../examples/charts/LineChartStreaming?raw";

<div className="overflow-hidden mt-20">
  <div className="w-101% grid  divide-border [grid-template-columns:repeat(auto-fit,minmax(480px,1fr))] -mt-px -mb-px">
    <PopupLayoutPreview>
      <span>A donut chart that includes a total in the middle.</span>
      <CodeBlock code={{ main: donutChartTotal }} layout="popup" />
    </PopupLayoutPreview>
    <PopupLayoutPreview>
      <span>A gauge charte that includes a threshold indicator.</span>
      <CodeBlock code={{ main: gaugeChart }} layout="popup" />
    </PopupLayoutPreview>
    <PopupLayoutPreview>
      <span>A line chart with streaming data.</span>
      <CodeBlock code={{ main: lineChartStreamingCode }} layout="popup" />
    </PopupLayoutPreview>
  </div>
</div>
