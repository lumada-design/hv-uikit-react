import { CodeBlock } from "../../components/code/CodeBlock";

import copyButton from "../../examples/inputs/CopyButton?raw";
import dropdownPrefix from "../../examples/inputs/DropdownPrefix?raw";
import editableValue from "../../examples/inputs/EditableInput?raw";
import tagsDropdown from "../../examples/inputs/TagsDropdown?raw";
import tagsSuggestions from "../../examples/inputs/TagsSuggestions?raw";

# Inputs examples

<div className="overflow-hidden">
  <div className="w-101% grid  divide-border [grid-template-columns:repeat(auto-fit,minmax(360px,1fr))] -mt-px -mb-px">
    <CodeBlock id="1" code={{ main: dropdownPrefix }} layout="popup" />
    <CodeBlock id="2" code={{ main: copyButton }} layout="popup" />
    <CodeBlock id="3" code={{ main: tagsDropdown }} layout="popup" />
    <CodeBlock id="4" code={{ main: tagsSuggestions }} layout="popup" />
    <CodeBlock id="5" code={{ main: editableValue }} layout="popup" />
  </div>
</div>
