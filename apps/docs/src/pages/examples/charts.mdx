import { CodeBlock } from "../../components/code/CodeBlock";

import donutChartTotal from "../../examples/charts/DonutChartTotal?raw";
import heatmapData from "../../examples/charts/heatmapData?raw";
import heatmapImageCode from "../../examples/charts/HeatmapImage?raw";
import lineChartStreamingCode from "../../examples/charts/LineChartStreaming?raw";

# Charts examples

## Line chart with streaming

A line chart with regular interval updates.

<CodeBlock code={{ main: lineChartStreamingCode }} />

## Donut chart with total inside

A donut chart that shows the total in the center

<CodeBlock code={{ main: donutChartTotal }} />

## Heatmap over image

A heatmap overlayed over an image.

<CodeBlock code={{ main: heatmapImageCode, "./heatmapData": heatmapData }} />
