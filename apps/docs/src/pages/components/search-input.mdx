import { Callout } from "nextra/components";
import { HvSearchInput, inputClasses } from "@hitachivantara/uikit-react-core";

import { getComponentData } from "../../utils/component";

import { Playground } from "../../components/code/Playground";
import { Header } from "../../components/Header";

export const getStaticProps = async ({ params }) => {
  const meta = await getComponentData("SearchInput", "core", inputClasses);
  return { props: { ssg: { meta } } };
};

<Header />

<Playground
  Component={HvSearchInput}
  componentName="HvSearchInput"
  controls={{
    label: { type: "text", defaultValue: "Search" },
    description: { type: "text", defaultValue: "Please enter a search term" },
    placeholder: "Insert search term...",
    required: { defaultValue: true },
    readOnly: { defaultValue: false },
    disabled: { defaultValue: false },
  }}
  decorator={(c) => <div className="w-full">{c}</div>}
/>

<br />

<Callout type="info">
  The `HvSearchInput` component shares the same API as the `HvInput` component.
  Please check the [HvInput component page](/components/input) for
  documentation.
</Callout>
