import { HvRadio, radioClasses } from "@hitachivantara/uikit-react-core";

import { getComponentData } from "../../utils/component";

import { Playground } from "../../components/code/Playground";
import { Header } from "../../components/Header";

export const getStaticProps = async ({ params }) => {
  const meta = await getComponentData("Radio", "core", radioClasses);
  return { props: { ssg: { meta } } };
};

<Header />

<Playground
  Component={HvRadio}
  componentName="HvRadio"
  controls={{
    required: { defaultValue: true },
    disabled: { defaultValue: false },
    semantic: { defaultValue: false },
    color: { type: "color" },
  }}
  componentProps={{
    label: "I have read and agree to the terms and conditions",
    statusMessage: "Please accept the terms and conditions",
    defaultChecked: false,
    indeterminate: false,
  }}
/>

### Color

You can use the `color` prop to style the color of the radio. It accepts any UI Kit or CSS color.

```tsx live
<div className="flex gap-sm">
  <HvRadio checked color="primary" />
  <HvRadio checked color="positive" />
  <HvRadio checked color="coral" />
  <HvRadio checked color="#c73aa8" />
</div>
```

### Controlled

Controlled radio button. Clicking the Radio button 1 does nothing, while clicking Radio button 2 changes both inputs.

```tsx live
import { useState } from "react";

export default function Demo() {
  const [isChecked, setIsChecked] = useState(false);

  return (
    <div className="flex gap-xs">
      <HvRadio checked={isChecked} label="Radio 1" />
      <HvRadio
        checked={isChecked}
        onChange={(_, checked) => setIsChecked(checked)}
        label="Radio 2"
      />
    </div>
  );
}
```

### Related components

- [`HvFormElement`](/components/form-element)
- [`HvRadioGroup`](/components/radio-group)
