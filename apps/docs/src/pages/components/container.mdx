import {
  containerClasses,
  HvContainer,
  HvPanel,
} from "@hitachivantara/uikit-react-core";

import Playground from "@docs/components/code/Playground";
import { Header } from "@docs/components/Header";
import { getComponentData } from "@docs/utils/component";

export const getStaticProps = async ({ params }) => {
  const meta = await getComponentData("Container", "core", containerClasses);
  return { props: { ssg: { meta } } };
};

<Header />

<Playground
  Component={HvContainer}
  componentName="HvContainer"
  controls={{
    maxWidth: { defaultValue: "sm" },
    fixed: { defaultValue: false },
    disableGutters: { defaultValue: false },
  }}
>
  <HvPanel className="h-100px">Content</HvPanel>
</Playground>
