---
title: "KPIs"
---

import { CodeBlock } from "../../../components/code/CodeBlock";

import dataLoading from "./DataLoading?raw";
import selectable from "./Selectable?raw";
import semantic from "./Semantic?raw";
import smallDonut from "./SmallDonut?raw";
import statusIcon from "./StatusIcon?raw";

### KPI Cards

Discover various KPI card layouts and features to highlight important metrics in your UI.

### Color Variants

Showcase of KPI cards with different border colors to indicate status or category.

<CodeBlock layout="expandable" code={semantic} />

### Status Icons

Showcase of KPI cards with different status icons.

<CodeBlock layout="expandable" code={statusIcon} />

### Selectable Cards

KPI cards with radio selection support—ideal for interactive dashboards or comparisons.

<CodeBlock layout="expandable" code={selectable} />

### Dynamic KPI Value

Live KPI card that fetches and displays data based on a selected endpoint, including fallback/error handling.

<CodeBlock layout="expandable" code={dataLoading} />

### Small Donut KPI

Small custom donut indicators for displaying percentage values, useful for compact KPI displays. No charts used, just SVG circles.

<CodeBlock layout="expandable" code={smallDonut} />
