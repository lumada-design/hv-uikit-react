import { w } from "happy-dom/lib/PropertySymbol.js";

import { CodeBlock } from "../../../components/code/CodeBlock";
import plusTags from "./PlusTags?raw";

### Tags Components

A set of examples demonstrating the usage of the Tags component in various containers.

<div className="overflow-hidden mt-20">
  <div className="w-101% grid divide-border [grid-template-columns:repeat(auto-fit,minmax(480px,1fr))] -mt-px -mb-px [&>*]:min-h-[200px]">
    <CodeBlock
      code={plusTags}
      layout="popup"
      title="Tags on limited container with plus indicator (resizable container)"
    />
  </div>
</div>
