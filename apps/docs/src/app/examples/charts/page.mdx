import { CodeBlock } from "../../../components/code/CodeBlock";
import donutChartTotal from "./DonutChartTotal?raw";
import gaugeChart from "./GaugeChart?raw";
import lineChartStreamingCode from "./LineChartStreaming?raw";

### Charts

Explore chart component usage and variations, ideal for dashboards and data visualization needs.

<div className="overflow-hidden mt-20">
  <div className="w-101% grid divide-border [grid-template-columns:repeat(auto-fit,minmax(480px,1fr))] -mt-px -mb-px [&>*]:min-h-[200px]">
    <CodeBlock
      code={{ main: donutChartTotal }}
      layout="popup"
      title="Sales by Country"
    />
    <CodeBlock
      code={{ main: gaugeChart }}
      layout="popup"
      title="Risk Assessment"
    />
    <CodeBlock
      code={{ main: lineChartStreamingCode }}
      layout="popup"
      title="Data Streaming"
    />
  </div>
</div>
