import { accordionClasses } from "@hitachivantara/uikit-react-core";

import { Layout,  Demo } from "../../components"

import { getComponentData, getComponentSamples } from "apps/docs/utils";

export const getStaticProps = async ({ params }) => {

    const meta = await getComponentData("Accordion",accordionClasses)

return getComponentSamples("Accordion",["Disabled","Controlled"]).then((samples) => {

    console.log(samples)
    return {
      props: {
        ssg: {
          meta: meta,
          samples: samples,
        },
      },
    }

});

}

<Layout>

### Disabled

<Demo name="Disabled" />

### Controlled

<Demo name="Controlled" />

</Layout>
