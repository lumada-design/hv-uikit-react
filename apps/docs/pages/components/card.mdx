import { cardClasses } from "@hitachivantara/uikit-react-core";

import { Layout,  Demo } from "../../components"

import { getComponentData, getComponentSamples } from "apps/docs/utils";

export const getStaticProps = async ({ params }) => {

    const meta = await getComponentData("Card", cardClasses)

return getComponentSamples("Card",[]).then((samples) => {

    console.log(samples)
    return {
      props: {
        ssg: {
          meta: meta,
          samples: samples,
        },
      },
    }

});

}

<Layout>

### All Components

{/* <Demo name="AllComponets" /> */}

</Layout>
