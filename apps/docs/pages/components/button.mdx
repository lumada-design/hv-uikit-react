import { buttonClasses } from "@hitachivantara/uikit-react-core";

import { Layout,  Demo } from "../../components"

import { getComponentData, getComponentSamples } from "apps/docs/utils";

export const getStaticProps = async ({ params }) => {

const meta = await getComponentData("Accordion", buttonClasses)

return getComponentSamples("Button",["Variants", "Icons"]).then((samples) => {

    return {
      props: {
        ssg: {
          meta: meta,
          samples: samples,
        },
      },
    }

});

}

<Layout>

### Usage

```jsx live
<>
  <HvButton variant="primarySubtle">Primary</HvButton>
  <HvButton variant="secondarySubtle">Secondary</HvButton>
</>
```

### Variants

<Demo name="Variants" />

### Icons

<Demo name="Icons" />

</Layout>
