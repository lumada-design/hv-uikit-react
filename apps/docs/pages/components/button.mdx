import { promises as fs } from "fs";

import { Layout } from "../../components/Layout"

import { Demo } from "../../components/Demo"

import { Variants, Icons } from "../../../../docs/samples/Button"

import { getComponentData } from "apps/docs/utils";

export const getStaticProps = async ({ params }) => {

const meta = await getComponentData("Accordion")

return fs.readFile(`./../../docs/samples/Button/Variants.tsx`, "utf8").then((values) => {
return {
props: {
ssg: {
meta: meta,
demo: values
},
},
}
});

}

<Layout>

```jsx live
<>
  <Button>Primary</Button>
  <Button variant="secondary">Secondary</Button>
</>
```

### Variants

<Variants />
<Demo />

### Icons

<Icons />

</Layout>
