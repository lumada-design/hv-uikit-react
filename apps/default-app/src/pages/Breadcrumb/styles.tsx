import styled from "@emotion/styled";
import { HvGrid, theme } from "@hitachivantara/uikit-react-core";

const StyleGrid = styled(HvGrid)({
  gap: theme.space.xs,
  padding: 0,
});

export default StyleGrid;
